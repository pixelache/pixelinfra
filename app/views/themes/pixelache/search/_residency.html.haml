.residency_hit
  .title= hit.name
  .date= date_range(hit.start_at, hit.end_at)
  .excerpt= raw truncate_html(sanitize(hit.description, tags: %w{b bold strong i em italic u underline}), :length => 300)
  .read_more= link_to t(:read_more), hit